# Gator CLI

A simple command-line RSS feed manager built with Go and PostgreSQL.

## Requirements

Make sure you have the following installed on your machine:

- [Go](https://golang.org/dl/) (v1.20+)
- [PostgreSQL](https://www.postgresql.org/) (with a running instance)
- `git`

## Setup

1. **Clone the repo**:

   ```bash
   git clone https://github.com/anton-jj/gator.git
   cd gator

